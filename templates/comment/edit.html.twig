{% extends 'base.html.twig' %}

{% block title %}{{ site_name }}: Edit Comment{% endblock %}

{% block body %}
<main id="main">

    <!-- ======= Blog Section ======= -->
    <section class="breadcrumbs">
        <div class="container">

            <div class="d-flex justify-content-between align-items-center">
                <h2>Comment</h2>

                <ol>
                    <li><a href="{{ path('home') }}">{% trans %}Home{% endtrans %}</a></li>
                    <li><a href="{{ path('admin') }}">Admin</a></li>
                    <li><a href="{{ path('comment_admin_index') }}">{% trans %}Comment{% endtrans %}</a></li>
                    <li>{% trans %}Comment index{% endtrans %}</li>
                </ol>
            </div>

        </div>
    </section><!-- End Blog Section -->

    <!-- ======= Blog Section ======= -->
    <section id="blog" class="blog">
        <div class="container" data-aos="fade-up">

            <div class="row d-flex justify-content-center mb-5">
                <div class="col">
                    <h1>{% trans %}Edit Comment{% endtrans %}</h1>

                    {{ include('comment/_form.html.twig', {'button_label': 'Update'}) }}

                </div>
            </div>
        </div>
    </section>
</main>
{% endblock %}